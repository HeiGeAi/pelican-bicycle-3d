{
  "name": "pelican-bicycle-3d",
  "private": true,
  "version": "1.0.0",
  "description": "A pelican riding a bicycle around a procedurally generated tropical island - real-time 3D with Three.js.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "node --test tests/*.test.js",
    "test:train": "node --test tests/last-train-home.test.js",
    "build:train-art": "node scripts/build-last-train-art.mjs",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.186.0"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
